1. Folder Structure -> server, app.js, database, router, controller, middleware
2. Authenticatin


Morgen is a middleware which is used to track time of response
app.use(morgan("dev"));

redis -> used for caching

parseJSON -> it converts json data into string => user = JSON.parse(user)
JSON.Stringify(user) -> converts string data into json => JSON.stringify(user)